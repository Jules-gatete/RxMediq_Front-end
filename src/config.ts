export const API_BASE_URL = 'http://localhost:5000';

export const API_ENDPOINTS = {
  LIVE_DATA: `${API_BASE_URL}/live_data`,
  PREDICT: `${API_BASE_URL}/predict`,
  UPLOAD: `${API_BASE_URL}/upload`,
};